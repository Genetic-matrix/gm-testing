# Findings ledger

One row per finding. Status: `open` / `fix committed <sha>` / `cleared <date>` / `reopened <date>`.
Fix sessions set `fix committed`; only the nightly run sets `cleared` or `reopened`.
A cleared finding becomes a permanent check in `routine/nightly.js`.

| ID | First seen | Last seen | Tier | Area | Finding | Steps | Status |
|---|---|---|---|---|---|---|---|
| F-001 | 2026-09-23 | 2026-09-23 | all | console | CookieYes script throws on the staging hub: "Looks like your website URL has changed", banner registered to another URL. Staging only, probably; check live is clean. | Load `/user-hub-tailwind/` on staging, read console | open |
| F-002 | 2026-09-23 | 2026-09-23 | all | engine data | `chinese_astro` DB table has wrong rows for 1891, 1925 and 2026 (Vladimir, 23 Sep; his verify_chinese baseline deliberately diverges there). If anything reads that table for the Chinese sign, those birth years show the wrong animal; 2026 is the current year. | Ask Vladimir what reads `chinese_astro`; compare those 3 rows to verify_chinese.txt | open |
| F-003 | 2026-09-23 | 2026-09-23 | public | astro calendar | `TransitCalendarData` events (ingresses, stations, Sun line changes) cover a rolling 24 h from the requested instant and carry a time with no date, in UTC. For `date=2026-09-23T06:00Z` it lists "Moon Aquarius to Pisces 03:23", which is 24 Sep 03:23 UTC (Swiss Ephemeris agrees to the minute). Correct maths; the risk is a page reading it as 23 Sep 03:23, or as local time. Positions themselves: 16/16 bodies match (incl. Ceres, Pallas, Juno, Vesta). | Staging `TransitCalendarData?date=2026-09-23T06:00:00.000Z&method=Tropical&...&include_asteroids=true`; saved in `golden-master/calendar/` | open: check how the Astro Calendar page labels these |
| F-004 | 2026-09-23 | 2026-09-23 | all | token | Research copies the token once at page load (`gm-web` `hub-research.js:19`) and all 4 of its calls use plain `fetch`, not `GmHubToken.authedFetch`. When the token rolls, Research search dies while the rest of the hub recovers. John hit it on staging 23 Sep. Member standard: token death must be invisible, never a re-login. | nightly.js token test, scenario dead-token, Research step | fix committed 99ed8fdc |
| F-005 | 2026-09-23 | 2026-09-23 | all | token | Token renewal is reactive (only after a 401) and `gm_hub_refresh_token_ajax` requires the page nonce (`check_ajax_referer`). A WP nonce dies 12 to 24 h after page load, so on a hub left open that long every renewal is refused, and silently: `hub-token.js` reads the refusal, finds no token, retries with the dead one, member sees "Chart is not available". Same staleness that broke Logout (fixed there with a no-nonce endpoint), not fixed here. | nightly.js token test, scenario stale-nonce | open: fix committed aba4ebce + 517db856 (renew before expiry, 12 h cap, renew on wake, loud failures, backoff); step 3 nonce removal awaiting Joseph |
| F-006 | 2026-09-23 | 2026-09-23 | all | token | Discovery sweep: F-004 is not just Research. 41 API call sites in 10 files send `window.GM_TOKEN` with plain `fetch` and no renew-and-retry (hub-inline-1.js 17, hub-inline-0.js 7, template-user-hub-tailwind.php 6, hub-research.js 4, hub-chart-builder.js 2, one each in support-contact, location, inline-2, education, chart-styles). Only hub-data, hub-chart-cascade, nav and part of inline-1 use `GmHubToken`. Each of the 41 fails on first use after the token dies unless another call happened to renew it first. Includes settings save (calc method), add-person save, place search, cycles, chart builder, education, support contact. | `grep -rnE "Bearer .*window\.GM_TOKEN" gm-web/wp-content/themes/geneticmatrix` | fix committed 99ed8fdc (routing) + 74438237 (CI guard tools/check-hub-token-use.py) |
| F-007 | 2026-09-23 | 2026-09-23 | all | deploy | CONFIRMED cause of the 23 Sep staging outage (settings, chart systems, cycles, research 500 from 12:06 UTC): staging GmAPI `/Token` made POST-only (at my request, wrong order) while gmlaravelweb `GeneticMatrixLookupService::token()` still GETs; Laravel log shows only `HTTP request returned status code 405`. Leftover audit code was also firing on staging (spam, not the cause); AUDIT_ENABLED=false set by Joseph; audit project shelved permanently. | Joseph laravel.log grep 15:58; `curl -X GET https://api.staginggm.com/Token` -> 405 | open: unblock = Vladimir re-enables GET on staging, fix = gmlaravelweb POST login deployed staging then live BEFORE GmAPI POST-only reaches live |
| F-008 | 2026-09-23 | 2026-09-23 | all | security config | Staging Laravel returns full exception detail to the browser (class, server file path, line), so `APP_DEBUG=true` on staging. Tolerable on a private staging box; on live it would leak server paths and code on any error. Seen via GET `https://gmhub.staginggm.com/api/auth/wp-bootstrap` (405 is expected: POST-only by design). | Joseph: confirm `APP_DEBUG=false` in live `.env`; on the new staging keep it matching live | open |
| F-009 | 2026-09-23 | 2026-09-23 | sidereal systems | engine | Design time for sidereal charts changed between Vladimir's baseline and staging today. Baseline psId 339: design 17:19:11 UT = 88.000 deg of solar arc measured SIDEREALLY. Staging /MaintVerify: 17:23:51 = 88.000 deg measured TROPICALLY. 4.7 min shift; design Moon moves ~0.05 deg, enough to flip a line (sometimes gate/profile/definition) near a boundary, across the non-tropical calc systems. Which frame is intended is a methodology decision (Vladimir/John). Also: psId 339 settings on staging edited since baseline (house W to Placidus, skin 0 to 19), fixture must be locked. | `node golden-master/maintverify.js`; `golden-master/captures/2026-09-23/compare.md` | open: ask Vladimir which is intended, since when, is it live |
| F-010 | 2026-09-23 | 2026-09-23 | mobile | deploy | The mobile gate (phones get the classic hub) is on LIVE `functions.php` (~line 2903, confirmed by Joseph 16:48, phone shows old hub) but is NOT in git: only an uncommitted change in `gm-web` on John's laptop. Any deploy of `functions.php` from the repo would silently remove it and send phones to the new hub, which is desktop only. | `git -C gm-web diff -- wp-content/themes/geneticmatrix/functions.php` | open: hub session to commit it so the repo matches live |
